import React from "react";
import Button from "@mui/joy/Button";

export const FitButton: React.FC = () => (
    <Button fullWidth id="fit-button" disabled={false} type="submit">
        Fit Spectrum
    </Button>
);
